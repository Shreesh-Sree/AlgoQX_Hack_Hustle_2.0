# PostgreSQL connection string (required)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Port for the API server (default: 3001)
PORT=3001

# Frontend: URL of the deployed API server (leave empty for local proxy)
VITE_API_URL=
